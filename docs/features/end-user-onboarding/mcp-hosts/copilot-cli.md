# MCP host reference：GitHub Copilot CLI

> 對應 spec：`docs/features/end-user-onboarding/spec.md` § 2.2

## 狀態：自動寫入暫不支援

GitHub Copilot CLI（`gh copilot`）的 MCP server 接線格式 v1 尚未穩定，
本工具不自動寫入。`0ops mcp setup copilot-cli` 只會印本檔指引。

當 Copilot CLI 之 MCP config 規格穩定後，本檔將補完並啟用自動寫入路徑（spec § 2.2）。

## 替代方案

短期：在 Claude Code 或 Codex CLI 接 0ops（兩者皆有穩定 MCP 支援，見對應 reference）。

## 後續

追進度：
- GitHub Copilot CLI release notes
- `docs/0ops-plan.md` 之「依 MCP 支援度」三 AI CLI 矩陣
